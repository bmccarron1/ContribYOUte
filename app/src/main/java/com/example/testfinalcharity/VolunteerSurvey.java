package com.example.TestFinalCharity;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.util.Log;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.EditText;

import com.example.testfinalcharity.R;

public class VolunteerSurvey extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.content_volunteer_survey);
        Button name = findViewById(R.id.button);


        //Create array list of volunteers + radio list of skills


        //array list of charities, check box (multiple options), radio button (single option)
    }
}
